#include<bits/stdc++.h>
class Solution {
public:

    double myPow(double x, int n) {
       
        // if()
        long double result=pow(x,n);
        //  if(n==n/2)
        //  return n;

        // if(n>0)
        // {
        // long double ans=myPow(x,n-1);
        
        // long double result=x*ans;

        return result; 
        // }

        // else{
        //    long double ans=myPow(x,n+1);
        //    long double result=(1/x)*ans;

            // return result; 

        

    }
};